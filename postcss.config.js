// PostCSS configuration (ESM).
// Tailwind CSS + Autoprefixer rodam em build time para gerar o CSS final
// do Design System (ver tailwind.config.ts).
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};
